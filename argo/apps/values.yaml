spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  source:
    targetRevision: HEAD

  syncPolicy: {}

config:
  domain: localhost
  externalCert:
    projectId: example-project
