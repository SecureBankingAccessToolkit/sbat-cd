apiVersion: v1
kind: Secret
metadata:
  name: gcr-secret
  annotations:
    "helm.sh/hook": "pre-install"
    replicator.v1.mittwald.de/replicate-from: {{ .Values.imageRepoSecret.namespace }}/{{ .Values.imageRepoSecret.name }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: e30K